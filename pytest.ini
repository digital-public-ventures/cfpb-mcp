[pytest]
markers =
    unit: Fast, local unit tests (no network)
    integration: Integration tests (starts server and/or hits network)
    e2e: Paid, networked end-to-end tests (requires explicit opt-in)
